### CDN
内容分发网络，根据用户位置分配最近的资源。用户在上网的时候，不直接访问源站，而访问离他最近的边缘节点。本质是缓存了源站内容的代理服务器。

原理：
未使用CDN时，我们使用域名访问某个站点的路径为：
用户提交域名 -> 浏览器解析 -> DNS得到目的主机的IP地址 -> 向IP地址发出请求 -> 得到数据并回复

应用CDN后，DNS返回的不是IP地址，而是CNAME别名记录，只想CDN的全局负载均衡。