### 
- 21 合并两个有序链表
声明dummy，条件为while list1 !== null && list2 !== null，返回新建链表。

- 203 移除链表元素
声明dummy，条件为while dummy.next !== null，遇到相同值则将当前指针指向next.next，遇到其他值则正常位移，返回新建链表。

- 206 反转链表
声明prev，条件为while current !== null，返回prev。